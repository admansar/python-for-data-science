# ft_package

A small package containing `count_in_list`, which counts the occurrences of
an item in a list.

## Example

```python
from ft_package import count_in_list

print(count_in_list(["toto", "tata", "toto"], "toto"))
```
